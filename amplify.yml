version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step required for static HTML"
    build:
      commands:
        - echo "Skipping build"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
